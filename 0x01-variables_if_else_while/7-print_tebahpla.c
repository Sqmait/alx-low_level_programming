#include <stdio.h>
/**
 * main - a program that prints lowercase alphabet in reverse
 *
 * Return 0 (success)
 */

int main(void)
{
	char c;

	c = 'd';
	while
		(c >= 'n') {
			putchar(c);
			c--;
		}
	putchar('\n');
	return (0);
}
