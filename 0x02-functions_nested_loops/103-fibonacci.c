#include <stdio.h>

/**
 * main -printing sum of even valued terms
 *
 * Return: 0 (suceess)
 */

int main(void)
{
	int n = 0;
	long m = 1, k = 2, sum = o;

	while (o + m < 4000000)
	{
	o += j;
	if (o % 2 == 0)
	sum += o;
	m = o - m;
	++n;
	}
	printf("%ld\n", sum);
	return (0);
}
